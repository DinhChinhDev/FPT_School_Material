@page
@model PRN221_DinhChinh_Ass3.WebApp.Pages.Categories.IndexModel

@{
    ViewData["Title"] = "Index";
}
<link rel="stylesheet" href="~/css/StyleSheet1.css">
<h1>Index</h1>

<p>
    <a class="btn-action" asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Category[0].CategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Category) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                    <a class="btn-action" asp-page="./Edit" asp-route-id="@item.CategoryId">Edit</a> |
                    <a class="btn-action" asp-page="./Details" asp-route-id="@item.CategoryId">Details</a> |
                    <a class="btn-action" asp-page="./Delete" asp-route-id="@item.CategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
